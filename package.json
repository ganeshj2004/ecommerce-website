{
  "name": "aquacraft-bottle-ecommerce",
  "version": "1.0.0",
  "description": "Full-stack eCommerce website for a bottle-selling business with React, Express, MySQL, JWT, Cloudinary, and Razorpay.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server",
    "start:server": "npm run start --prefix server",
    "start:client": "npm run dev --prefix client"
  }
}
